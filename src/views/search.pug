form(action="/search").search__form
    input(placeholder="Search by title", name="keyword", type="text")
    input(type="submit").search__button
    span#searchBtn 
        i.fas.fa-search
        
    
